<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Seeker Details</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="bg-gray-900 text-white p-8">
    <h1 class="text-4xl font-bold">👤 <%= seeker.name %></h1>
    <p class="mt-2 text-gray-400">📧 Email: <%= seeker.email %></p>
    <p class="text-gray-400">
      📍 Location: <%= seeker.city %>, <%= seeker.state %>
    </p>

    <h2 class="text-2xl font-bold mt-6">🛠️ Repair Requests</h2>
    <% if (repairRequests.length > 0) { %>
    <ul class="mt-4 space-y-2">
      <% repairRequests.forEach(request => { %>
      <li class="bg-gray-800 p-4 rounded-lg">
        <strong><%= request.title %></strong> - <%= request.category %>
      </li>
      <% }); %>
    </ul>
    <% } else { %>
    <p class="text-gray-400">No requests found.</p>
    <% } %>
  </body>
</html>
